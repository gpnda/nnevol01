# NeuralNetwork.mutate() Test

Тест для проверки корректности работы метода `mutate()` класса `NeuralNetwork`.

## Что тестируется

- **Вероятность мутации**: проверяет что изменяется примерно заданный % весов
- **Визуализация**: выводит все изменённые веса с подсвечиванием
- **Сила мутации**: проверяет что изменения соответствуют параметру `mutation_strength`

## Запуск

```bash
python run_test_mutate.py
```

Или напрямую:
```bash
python tests/test_mutate.py
```

## Что выводит тест

### 1. **Параметры теста**
```
Mutation probability: 30.0%
Mutation strength: 0.5
```

### 2. **Информация о сети**
```
Network created with layers:
  Layer 1: (45, 50) weights + (50,) bias
  Layer 2: (50, 10) weights + (10,) bias
  Layer 3: (10, 3) weights + (3,) bias
```

### 3. **Сравнение весов**

Для каждого слоя выводит:
- **Original** — исходное значение веса
- **Mutated** — новое значение после мутации (красным если изменилось)
- **●** красная точка отмечает изменённые веса

Пример:
```
Weights Layer 1 (w1)
================================================================================
Shape: 45×50

Index      Original         Mutated          Changed?       
-----------------------------------------------
[0,5]        0.0123    →      0.3456  ●
[0,12]       0.0456    →      0.0987  ●
[1,3]        0.1234    →      0.2105  ●
...

Total changed: 689/2250 (30.6%)
```

### 4. **Статистика**

```
================================================================================
SUMMARY
================================================================================
Total parameters: 3613
Changed parameters: 1084
Change rate: 30.02%
Expected rate: 30.00%

✓ TEST PASSED
```

## Параметры для настройки

В файле `tests/test_mutate.py` строки 92-93:

```python
mutation_probability = 0.3  # 30% весов будут мутированы (попробовать 0.1, 0.5, и т.д.)
mutation_strength = 0.5     # Сила мутации (попробовать 0.1, 1.0, и т.д.)
```

## Цветовые коды

- 🟢 **Зелёный** — статистика и успешный результат
- 🔴 **Красный** — выделены изменённые веса
- 🔵 **Голубой** — заголовки разделов
- ⚪ **Белый** — обычный текст

## Интерпретация результатов

| Результат | Интерпретация |
|-----------|---------------|
| Change rate ≈ Expected rate | Мутация работает корректно ✓ |
| Change rate << Expected rate | Мутация слишком слабая |
| Change rate >> Expected rate | Вероятность мутации слишком высокая |
| ● много красных точек | Веса активно изменяются |
| ● мало красных точек | Мутация редко происходит |
