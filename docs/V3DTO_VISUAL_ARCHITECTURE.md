# V3DTO - Визуальная архитектура

## 🏗️ Архитектурная диаграмма (ASCII)

### СТАРАЯ АРХИТЕКТУРА (v2) - ❌ ПРОБЛЕМА

```
┌──────────────────────────────────────────────────────────────┐
│ Application Layer                                             │
│ ├─ Получает: world, app                                      │
│ └─ Передает в Renderer                                       │
└──────────────────────────────────────────────────────────────┘
                           ↓
┌──────────────────────────────────────────────────────────────┐
│ Renderer (v2)                                                │
│ ├─ Хранит: world, app                                       │
│ └─ Пробрасывает виджетам: world, debug, logger             │
│    ❌ Нет преобразования! Виджеты видят Domain данные!     │
└──────────────────────────────────────────────────────────────┘
    ↓                    ↓                    ↓
┌─────────────────┐  ┌──────────────────┐  ┌─────────────────┐
│ Viewport        │  │ VariablesPanel   │  │ SelectedCreature│
│ ├─ self.world   │  │ ├─ self.world    │  │ ├─ self.world   │
│ └─ debug →      │  │ └─ direct access │  │ ├─ logme →      │
│    (синглтон)   │  │    к параметрам  │  │ │ (синглтон)    │
│                 │  │                  │  │ └─ использует    │
└─────────────────┘  └──────────────────┘  │    методы       │
                                           └─────────────────┘

ПРОБЛЕМЫ:
❌ Каждый виджет имеет прямую ссылку на world
❌ Виджеты импортируют синглтоны (debug, logger)
❌ Невозможно тестировать без реального world
❌ Синглтоны создают скрытые зависимости
❌ Сложно менять источник данных
```

### НОВАЯ АРХИТЕКТУРА (v3dto) - ✅ РЕШЕНИЕ

```
┌──────────────────────────────────────────────────────────────┐
│ Application Layer                                             │
│ ├─ Получает: world, app, debugger, logger                   │
│ └─ Передает в Renderer                                       │
└──────────────────────────────────────────────────────────────┘
                           ↓
┌──────────────────────────────────────────────────────────────┐
│ Renderer (v3dto) - МЕДИАТОР                                  │
│ ├─ Хранит: world, app, debugger, logger                     │
│ ├─ Factory методы преобразуют данные в DTO:                 │
│ │  ├─ world → WorldStateDTO                                  │
│ │  ├─ debug → DebugDataDTO                                   │
│ │  ├─ logger → CreatureHistoryDTO                            │
│ │  └─ ...                                                     │
│ └─ Передает в виджеты: RenderStateDTO (ОДИН параметр)      │
│    ✅ Преобразование! Виджеты видят только DTO!            │
└──────────────────────────────────────────────────────────────┘
    ↓                    ↓                    ↓
┌─────────────────┐  ┌──────────────────┐  ┌─────────────────┐
│ Viewport        │  │ VariablesPanel   │  │ SelectedCreature│
│ ├─ render_state │  │ ├─ render_state  │  │ ├─ render_state │
│ └─ world_dto    │  │ └─ params_dto    │  │ └─ creature_dto │
│    raycast_dto  │  │    via callbacks │  │    history_dto  │
│                 │  │                  │  │                 │
└─────────────────┘  └──────────────────┘  └─────────────────┘

ПРЕИМУЩЕСТВА:
✅ Виджеты не имеют ссылки на world
✅ Виджеты не импортируют синглтоны
✅ Можно тестировать с mock DTO
✅ Явные контракты между слоями
✅ Легко менять источник данных
```

---

## 📊 Поток данных в каждом фрейме

```
ФРЕЙМ N:

┌─ Application.run()
│
├─ if is_running:
│  ├─ world.update()           ← Симуляция
│  ├─ world.update_map()       ← Обновление карты
│  └─ logme.write_stats()      ← Логирование статистики
│
├─ if animate_flag:
│  └─ Renderer.draw()          ← ГЛАВНЫЙ МЕТОД
│     │
│     ├─ _prepare_render_state_dto()  ← ПРЕОБРАЗОВАНИЕ
│     │  │
│     │  ├─ _prepare_world_dto()
│     │  │  ├─ world.creatures →  [CreatureDTO, ...]
│     │  │  ├─ world.foods →      [FoodDTO, ...]
│     │  │  └─ world.map →        np.ndarray
│     │  │
│     │  ├─ _prepare_debug_dto()
│     │  │  ├─ debug.get("raycast_dots") → DebugDataDTO.raycast_dots
│     │  │  ├─ debug.get("all_visions") → DebugDataDTO.all_visions
│     │  │  └─ debug.get("all_outs") →    DebugDataDTO.all_outs
│     │  │
│     │  ├─ _prepare_simulation_params_dto()
│     │  │  └─ sp.mutation_probability, sp.food_amount, ... → SimulationParamsDTO
│     │  │
│     │  └─ → RenderStateDTO (ГОТОВ)
│     │
│     └─ if current_state == 'main':
│        ├─ viewport.draw(screen, render_state)
│        ├─ selected_creature_panel.draw(screen, render_state)
│        └─ selected_creature_history.draw(screen, render_state)
│
└─ Renderer.control_run()  ← ОБРАБОТКА СОБЫТИЙ
   ├─ pygame.event.get()
   ├─ handle_keyboard(event)
   └─ handle_mouse(event)
```

---

## 🔄 Цикл жизни данных

```
ЭТАП 1: ПОЛУЧЕНИЕ ДАННЫХ
┌────────────────────────────────────┐
│ world ← содержит creatures, foods  │
│ debug ← содержит raycast_dots      │
│ logger ← содержит energy_history   │
│ simparams ← содержит все параметры │
└────────────────────────────────────┘
         ↓ (в Renderer.draw())

ЭТАП 2: ПРЕОБРАЗОВАНИЕ
┌────────────────────────────────────┐
│ _prepare_world_dto()               │
│ _prepare_debug_dto()               │
│ _prepare_simulation_params_dto()   │
│ _prepare_creature_history_dto()    │
│ ...                                │
│ ↓                                  │
│ RenderStateDTO (структурированный) │
└────────────────────────────────────┘
         ↓

ЭТАП 3: РАСПРЕДЕЛЕНИЕ
┌────────────────────────────────────┐
│ render_state → viewport.draw()     │
│ render_state → panel.draw()        │
│ render_state → history.draw()      │
│                                    │
│ Виджеты используют ТОЛЬКО DTO!     │
└────────────────────────────────────┘
         ↓

ЭТАП 4: ОТРИСОВКА
┌────────────────────────────────────┐
│ pygame.display.flip()              │
│ ← Результат на экран               │
└────────────────────────────────────┘
```

---

## 📦 DTO иерархия с примерами

```
RenderStateDTO
├── world: WorldStateDTO
│   ├── map: np.ndarray
│   │   ├── [0] = пусто
│   │   ├── [1] = стена
│   │   ├── [2] = пища
│   │   └── [3] = существо
│   ├── width: 100
│   ├── height: 50
│   ├── creatures: [CreatureDTO, CreatureDTO, ...]
│   │   ├── id: 1
│   │   ├── x: 42.5
│   │   ├── y: 23.1
│   │   ├── angle: 1.57
│   │   ├── energy: 0.75
│   │   └── ...
│   ├── foods: [FoodDTO, FoodDTO, ...]
│   │   ├── x: 10.0
│   │   ├── y: 15.0
│   │   └── energy: 0.5
│   └── tick: 1250
│
├── params: SimulationParamsDTO
│   ├── mutation_probability: 0.01
│   ├── mutation_strength: 2.5
│   ├── creature_max_age: 5000
│   ├── food_amount: 150
│   ├── is_running: True
│   ├── is_animating: True
│   └── ... (еще 10+ параметров)
│
├── debug: DebugDataDTO
│   ├── raycast_dots: np.ndarray (N, 2)
│   ├── all_visions: np.ndarray (M, 15)
│   └── all_outs: np.ndarray (P, 3)
│
├── selected_creature: SelectedCreaturePanelDTO
│   ├── creature: CreatureDTO (id=1, x=42.5, ...)
│   └── history: CreatureHistoryDTO
│       ├── creature_id: 1
│       ├── energy_history: [0.9, 0.88, 0.85, ..., 0.75]
│       ├── events: [CreatureEventDTO, ...]
│       │   ├── tick: 100
│       │   ├── event_type: "EAT_FOOD"
│       │   └── value: 0.2
│       └── energy_min: 0.5, energy_max: 1.0, ...
│
├── current_state: "main"
├── tick: 1250
└── fps: 60
```

---

## 🔗 Как виджеты используют DTO

```
VIEWPORT:
┌─────────────────────────────────────┐
│ def draw(screen, render_state):     │
│   world_dto = render_state.world    │
│   debug_dto = render_state.debug    │
│                                     │
│   for creature in world_dto.creatures:
│     viewport_pos = map_to_viewport( │
│       creature.x,                   │
│       creature.y                    │
│     )                               │
│     pygame.draw.rect(              │
│       screen, (50,50,255),          │
│       viewport_pos, size            │
│     )                               │
│                                     │
│   if debug_dto.raycast_dots:        │
│     for dot in debug_dto.raycast:   │
│       pygame.draw.circle(...)       │
└─────────────────────────────────────┘

SELECTED CREATURE PANEL:
┌─────────────────────────────────────┐
│ def draw(screen, render_state):     │
│   creature_dto = (                  │
│     render_state                    │
│     .selected_creature              │
│     .creature                       │
│   )                                 │
│                                     │
│   text = (                          │
│     f"ID: {creature_dto.id}\n"      │
│     f"Energy: {creature_dto.energy" │
│     f"\nAge: {creature_dto.age}"     │
│   )                                 │
│   render_text(screen, text)         │
└─────────────────────────────────────┘

SELECTED CREATURE HISTORY:
┌─────────────────────────────────────┐
│ def draw(screen, render_state):     │
│   history_dto = (                   │
│     render_state                    │
│     .selected_creature              │
│     .history                        │
│   )                                 │
│                                     │
│   for i, energy in enumerate(      │
│     history_dto.energy_history      │
│   ):                                │
│     y = int(energy * 100)           │
│     pygame.draw.line(               │
│       screen,                       │
│       (0, 255, 0),                  │
│       (i, prev_y),                  │
│       (i, y)                        │
│     )                               │
└─────────────────────────────────────┘
```

---

## 🔀 Сравнение: v2 vs v3dto

```
╔════════════════════════════════════════════════════════════╗
║                         v2 (Старая)                        ║
╠════════════════════════════════════════════════════════════╣
║                                                            ║
║ Viewport.__init__(self, world=None):                       ║
║     self.world = world  ❌ Прямая зависимость             ║
║                                                            ║
║ Viewport.draw(self, screen, font, ...):                    ║
║     for creature in self.world.creatures:                  ║
║         ...                                                ║
║     raycast_dots = debug.get(...)  ❌ Синглтон            ║
║                                                            ║
║ ПРОБЛЕМЫ:                                                  ║
║ ❌ Нельзя тестировать без world                           ║
║ ❌ Синглтоны - скрытые зависимости                        ║
║ ❌ Сложно менять источник данных                          ║
║                                                            ║
╚════════════════════════════════════════════════════════════╝

╔════════════════════════════════════════════════════════════╗
║                      v3dto (Новая)                         ║
╠════════════════════════════════════════════════════════════╣
║                                                            ║
║ Viewport.__init__(self):                                   ║
║     pass  ✅ Никаких зависимостей                         ║
║                                                            ║
║ Viewport.draw(self, screen, render_state):                ║
║     world_dto = render_state.world                        ║
║     for creature in world_dto.creatures:  ✅ DTO          ║
║         ...                                                ║
║     debug_dto = render_state.debug                        ║
║     if debug_dto.raycast_dots:  ✅ DTO, не синглтон      ║
║                                                            ║
║ ПРЕИМУЩЕСТВА:                                              ║
║ ✅ Можно тестировать с mock DTO                          ║
║ ✅ Явные зависимости через параметры                     ║
║ ✅ Легко менять источник данных                          ║
║                                                            ║
╚════════════════════════════════════════════════════════════╝
```

---

## 📋 Матрица миграции

```
ВИДЖЕТ              │ v2 ЗАВИСИМОСТИ │ v3dto DTO        │ СЛОЖНОСТЬ
─────────────────────┼─────────────────┼──────────────────┼──────────
Viewport            │ world, debug    │ RenderStateDTO   │ ███░░░░
                    │                 │ - world_dto      │ (средняя)
                    │                 │ - debug_dto      │
─────────────────────┼─────────────────┼──────────────────┼──────────
VariablesPanel      │ world, app      │ RenderStateDTO   │ █████░░░
                    │                 │ - params_dto     │ (сложная)
                    │                 │ + callbacks      │
─────────────────────┼─────────────────┼──────────────────┼──────────
SelectedCreature    │ world           │ RenderStateDTO   │ ██░░░░░░
                    │                 │ - creature_dto   │ (простая)
                    │                 │ - world_dto      │
─────────────────────┼─────────────────┼──────────────────┼──────────
SelectedCreature    │ world, logger   │ RenderStateDTO   │ ███░░░░░
History             │                 │ - history_dto    │ (средняя)
                    │                 │ (с events)       │
```

---

## 🎯 Преимущества визуально

```
СЛАБАЯ СВЯЗАННОСТЬ:
┌──────────┐
│ Renderer │  Только Renderer знает о world/debug/logger
└────┬─────┘
     │ (преобразует в DTO)
     ↓
┌──────────────────┐
│ RenderStateDTO   │  Виджеты видят только DTO
└────┬─────────────┘
     ├─ Viewport
     ├─ VariablesPanel
     ├─ SelectedCreaturePanel
     └─ SelectedCreatureHistory
     
     ▲ Все виджеты полностью изолированы друг от друга

ПОЛНАЯ ТЕСТИРУЕМОСТЬ:
┌──────────────┐
│ Mock DTO     │
└────┬─────────┘
     │
     ├─→ Viewport (работает!)
     ├─→ VariablesPanel (работает!)
     ├─→ SelectedCreaturePanel (работает!)
     └─→ SelectedCreatureHistory (работает!)
     
     ▲ БЕЗ реального world!

ГИБКОСТЬ ИСТОЧНИКА:
┌─────────────┐     ┌───────────┐     ┌──────────┐
│ Real World  │     │ Saved Game│     │  Replay  │
└────┬────────┘     └────┬──────┘     └────┬─────┘
     │                   │                 │
     └─────────┬─────────┴─────────────────┘
               ↓
         ┌──────────┐
         │ RenderStateDTO
         └────┬─────┘
              │
         Viewport (same code!)
```

---

## 🚀 Заключение

```
v2:  Renderer → [мир] → Viewport
                ↓
              БЕЗ преобразований → Множество зависимостей

v3dto: Renderer → [преобразование] → RenderStateDTO → Viewport
                                       ↑
                      Явные контракты, полная изоляция
```

✨ **DTO архитектура = чистый, тестируемый, масштабируемый код!**
