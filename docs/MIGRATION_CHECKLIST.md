# 📋 Чеклист: Миграция остальных модальных окон в v3dto

## Текущий статус проекта

### ✅ Завершено
- [x] **CreaturesListModal** (gui_creatures_list.py)
  - Инициализирован в Renderer
  - Обработка клавиатуры реализована
  - Отрисовка реализована
  - Полная документация

### ⏳ В очереди

## 1️⃣ LogsModal

**Текущие файлы**:
- `renderer/v1/gui_logs.py` (старая версия)
- `docs/` (нет документации)

**Что нужно сделать**:
- [ ] Создать `renderer/v3dto/gui_logs_modal.py`
- [ ] Адаптировать под RenderStateDTO
- [ ] Получать данные из logger через `render_state`
- [ ] Интегрировать в Renderer
  - [ ] Добавить импорт
  - [ ] Инициализировать в `__init__()`
  - [ ] Обновить `_handle_keyboard_logs()`
  - [ ] Обновить `_draw_logs()`
  - [ ] Добавить `_on_state_enter('logs')`
- [ ] Написать документацию
- [ ] Протестировать

**Ключевые особенности**:
- Отображение всех логов в таблице
- Навигация по логам (UP/DOWN, PAGE_UP/PAGE_DOWN)
- Возможно поиск по фильтрам
- Экспорт в файл

**v3dto адаптация**:
```python
class LogsModal:
    def __init__(self):
        # БЕЗ параметров
        self.surface = pygame.Surface(...)
        self.font = ...
    
    def draw(self, screen, render_state: RenderStateDTO):
        # render_state.params содержит доступ к logger
        # Получаем логи через render_state
        pass
```

---

## 2️⃣ ExperimentModal

**Текущие файлы**:
- `renderer/v1/` (возможно в какого-то файла)
- Нет явной реализации

**Что нужно сделать**:
- [ ] Определить назначение ExperimentModal
  - Запуск экспериментов?
  - Отслеживание метрик?
  - Конфигурация эксперимента?
- [ ] Создать `renderer/v3dto/gui_experiment_modal.py`
- [ ] Спроектировать UI/UX
- [ ] Интегрировать в Renderer
  - [ ] Добавить импорт
  - [ ] Инициализировать в `__init__()`
  - [ ] Обновить `_handle_keyboard_experiment()`
  - [ ] Обновить `_draw_experiment()`
  - [ ] Добавить `_on_state_enter('experiment')`
- [ ] Написать документацию

**Возможные компоненты**:
- Выбор типа эксперимента
- Конфигурация параметров
- Запуск/остановка
- Отслеживание прогресса
- Результаты

---

## 3️⃣ CreatureDetailsModal (новый)

**Статус**: Идея для расширения

**Что это**:
- Подробная информация о выбранном существе
- Открывается из CreaturesListModal (нажимая Enter или D)
- Показывает NN архитектуру, параметры, историю

**Что нужно сделать**:
- [ ] Определить содержимое панели
- [ ] Спроектировать UI
- [ ] Создать `renderer/v3dto/gui_creature_details_modal.py`
- [ ] Интегрировать в Renderer
- [ ] Написать документацию

**Возможные компоненты**:
- Информация о существе
- Архитектура нейросети (визуально)
- История энергии (график)
- Сенсорные входы (визуально)
- Потомки существа

---

## 4️⃣ StatisticsModal (новый)

**Статус**: Идея для расширения

**Что это**:
- Глобальная статистика по популяции
- Графики эволюции
- Средние метрики

**Что нужно сделать**:
- [ ] Определить метрики
- [ ] Спроектировать UI
- [ ] Создать `renderer/v3dto/gui_statistics_modal.py`
- [ ] Интегрировать в Renderer
- [ ] Написать документацию

**Возможные компоненты**:
- Размер популяции
- Средняя энергия
- Средний возраст
- Средняя скорость
- Количество поколений
- Динамика рождения/смерти

---

## Процесс миграции (шаблон)

Используйте этот процесс для каждого нового модального окна:

### Фаза 1: Планирование
```markdown
- [ ] Определён назначение окна
- [ ] Определён контент (что отображать)
- [ ] Определена навигация (какие клавиши)
- [ ] Скетч UI (размер, цвета, макет)
```

### Фаза 2: Разработка v3dto компонента
```markdown
- [ ] Создан файл renderer/v3dto/gui_*_modal.py
- [ ] Следуют v3dto паттерны:
  - [ ] Constants configuration
  - [ ] Zero-dependency init
  - [ ] draw(screen, render_state)
  - [ ] DTO isolation
- [ ] Реализована обработка навигации (методы move_*, reset, etc.)
- [ ] Проверена отрисовка
```

### Фаза 3: Интеграция в Renderer
```markdown
- [ ] Добавлен импорт в renderer.py
- [ ] Инициализирован в __init__()
- [ ] Добавлен обработчик клавиатуры _handle_keyboard_*()
- [ ] Добавлена функция отрисовки _draw_*()
- [ ] Добавлена логика _on_state_enter()
- [ ] Добавлена горячая клавиша (F1, F9, F12, и т.д.)
```

### Фаза 4: Документация
```markdown
- [ ] Migration doc (как перенесено из старой версии или создано заново)
- [ ] Usage examples (примеры использования)
- [ ] Architecture comparison (если было раньше, сравнение v1 vs v3dto)
- [ ] Quick start guide (быстрый старт)
```

### Фаза 5: Тестирование
```markdown
- [ ] Открывается по горячей клавише
- [ ] Навигация работает
- [ ] Данные отображаются правильно
- [ ] Закрывается по ESC
- [ ] Состояние правильно сбрасывается при повторном открытии
```

---

## Шаблон файла для нового модального окна

```python
# -*- coding: utf-8 -*-
"""
[ComponentName]Modal - v3dto версия.

[Описание компонента - что это, когда используется]

АРХИТЕКТУРА v3dto:
- НЕ имеет зависимостей от world, logger, debugger
- Получает данные только через RenderStateDTO
- Полностью изолирована от singleton'ов
"""

import pygame
from typing import TYPE_CHECKING

if TYPE_CHECKING:
    from renderer.v3dto.dto import RenderStateDTO


class [ComponentName]Modal:
    """
    [Описание класса]
    
    Архитектура DTO:
    - Получает RenderStateDTO в методе draw()
    - Извлекает нужные данные
    - Полностью изолирована от сингльтонов
    """
    
    # Координаты и размеры
    MODAL_X = 50
    MODAL_Y = 50
    WIDTH = 600
    HEIGHT = 400
    
    # Цвета
    COLORS = {
        'background': (30, 30, 30),
        'border': (150, 150, 150),
        'text': (200, 200, 200),
    }
    
    # Размеры
    FONT_SIZE = 16
    FONT_PATH = './tests/Ac437_Siemens_PC-D.ttf'
    
    def __init__(self):
        """Инициализация - БЕЗ параметров!"""
        self.surface = pygame.Surface((self.WIDTH, self.HEIGHT), pygame.SRCALPHA)
        
        try:
            self.font = pygame.font.Font(self.FONT_PATH, self.FONT_SIZE)
        except (FileNotFoundError, pygame.error):
            self.font = pygame.font.Font(None, self.FONT_SIZE)
    
    def draw(self, screen: pygame.Surface, render_state: 'RenderStateDTO') -> None:
        """Отрисовка модального окна."""
        # Очистка поверхности
        self.surface.fill(self.COLORS['background'])
        
        # Отрисовка контента (используя render_state)
        # ...
        
        # Копирование на экран
        screen.blit(self.surface, (self.MODAL_X, self.MODAL_Y))
    
    def reset(self) -> None:
        """Сброс состояния при открытии модала."""
        pass
```

---

## Горячие клавиши (пока)

| Клавиша | Состояние | Действие |
|---------|-----------|----------|
| F1 | main | creatures_list |
| F9 | main | popup_simparams |
| F12 | main | logs |
| ESC/F1 | creatures_list | main |
| ESC/F9 | popup_simparams | main |
| ESC/F12 | logs | main |

**Нужны новые клавиши?**
- [ ] Добавить LogsModal (F12 - занята)
- [ ] Добавить ExperimentModal (F2? Ctrl+E?)
- [ ] Добавить CreatureDetailsModal (Enter в CreaturesListModal)
- [ ] Добавить StatisticsModal (F10?)

---

## Приоритет разработки

### 🔴 Высокий приоритет
1. **LogsModal** - Уже была, нужна миграция

### 🟡 Средний приоритет
2. **ExperimentModal** - Неясна функциональность, нужна спецификация

### 🟢 Низкий приоритет
3. **CreatureDetailsModal** - Хорошо иметь, но не критично
4. **StatisticsModal** - Красивая фишка, но не критично

---

## Зависимости между компонентами

```
Renderer (state machine)
├── CreaturesListModal ✅ DONE
│   └─ Может открыть → CreatureDetailsModal (future)
├── LogsModal ⏳ TODO
├── ExperimentModal ⏳ TODO
├── CreatureDetailsModal (future)
│   └─ Может заполнить → StatisticsModal (future)
└── StatisticsModal (future)
```

---

## Контрольный список для каждого нового модала

- [ ] Файл создан в `renderer/v3dto/gui_*_modal.py`
- [ ] Следует v3dto паттернам (Constants, Zero-Deps Init, draw(), DTO Isolation)
- [ ] Импортирован в `renderer.py`
- [ ] Инициализирован в `Renderer.__init__()`
- [ ] Обработчик клавиатуры реализован
- [ ] Функция отрисовки реализована
- [ ] Логика `_on_state_enter()` реализована
- [ ] Документация написана (Migration, Examples, Architecture comparison)
- [ ] Протестировано в runtime
- [ ] Внесено в таблицу горячих клавиш
- [ ] Добавлено в диаграмму переходов состояний

---

## Ресурсы

- **v3dto паттерны**: [V3DTO_WIDGET_DOCUMENTATION_MANIFEST.md](V3DTO_WIDGET_DOCUMENTATION_MANIFEST.md)
- **Пример CreaturesListModal**: [CREATURES_LIST_MIGRATION.md](CREATURES_LIST_MIGRATION.md)
- **Быстрый старт**: [CREATURES_LIST_QUICKSTART.md](CREATURES_LIST_QUICKSTART.md)
- **Архитектура**: [CREATURES_LIST_ARCHITECTURE_COMPARISON.md](CREATURES_LIST_ARCHITECTURE_COMPARISON.md)

---

## Резюме

✅ **CreaturesListModal** - Завершена полная миграция и интеграция  
⏳ **LogsModal** - Следующий приоритет  
❓ **Остальные** - После определения функциональности

**Лучшие практики**: Используйте CreaturesListModal как образец для остальных модальных окон!
