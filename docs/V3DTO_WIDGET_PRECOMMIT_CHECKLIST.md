# V3DTO Widget Development - Pre-Commit Checklist

Финальный контрольный список перед коммитом нового виджета.

---

## ✅ Структура Файла

### Заголовок и Доска

```
☐ Файл создан в renderer/v3dto/gui_*.py
☐ Первая строка: # -*- coding: utf-8 -*-
☐ Docstring описывает виджет
☐ Docstring упоминает архитектуру v3dto
☐ Только один класс в файле
```

### Импорты

```
☐ import pygame
☐ from typing import ... (при необходимости)
☐ from renderer.v3dto.dto import RenderStateDTO (при необходимости)
☐ НЕТ импорта world, logger, debugger, simparams
☐ НЕТ импорта других виджетов
☐ TYPE_CHECKING для импортов в type hints
```

### Класс Виджета

```
☐ Имя класса в CamelCase
☐ Docstring с описанием роли
☐ КОНФИГУРАЦИЯ в виде class constants (в начале)
```

---

## 🎯 Конфигурация (Class Constants)

```
☐ WIDGET_X, WIDGET_Y определены (позиция на экране)
☐ WIDTH, HEIGHT определены (размер)
☐ FONT_SIZE определен
☐ FONT_PATH = './tests/Ac437_Siemens_PC-D.ttf'
☐ COLORS = {...} (словарь с цветами)
☐ Другие размеры как constants (PADDING, BORDER_WIDTH и т.д.)
☐ Никаких "магических чисел" в методах
```

---

## 🔧 Инициализация (__init__)

```
☐ def __init__(self): НЕ принимает параметры (или только callback)
☐ Создан self.rect = pygame.Rect(...)
☐ Создана self.surface = pygame.Surface(...)
☐ Инициализирован шрифт с try-except:
    ☐ try блок с pygame.font.Font(FONT_PATH, FONT_SIZE)
    ☐ except блок с pygame.font.Font(None, FONT_SIZE)
☐ Если есть callback, сохранен как self.on_something
☐ Если есть состояние (editing, selected_index), инициализировано
```

---

## 🎨 Отрисовка (draw)

### Сигнатура

```
☐ def draw(self, screen: pygame.Surface, render_state: RenderStateDTO) -> None:
   ИЛИ (для модальных/независимых):
☐ def draw(self, screen: pygame.Surface) -> None:
   ИЛИ (для viewport):
☐ def draw(self, screen: pygame.Surface, render_state: RenderStateDTO, 
           font: pygame.font.Font = None) -> None:
```

### Реализация

```
☐ Очистить surface (fill или другое)
☐ Нарисовать границу (если нужна)
☐ Использовать COLORS из class constants
☐ Все текстовые координаты - через constants (PADDING, etc.)
☐ Использовать render_state ТОЛЬКО для чтения данных
☐ В конце: screen.blit(self.surface, (self.rect.x, self.rect.y))
☐ Никаких побочных эффектов (не менять состояние мира)
```

---

## 🖱️ Обработка Событий (если применимо)

```
☐ def handle_keydown(self, event: pygame.event.Event) -> bool:
   (если нужна клавиатура)
☐ def handle_mousebuttondown(self, event: pygame.event.Event) -> bool:
   (если нужна мышь)
☐ def handle_mousebuttonup(self, event: pygame.event.Event) -> bool:
   (если нужна мышь)
☐ Все методы возвращают bool (True если обработано)
☐ В конце возвращать False если не обработано
```

---

## 📝 Документация

```
☐ В начале файла docstring с описанием
☐ Docstring упоминает архитектуру v3dto
☐ Docstring говорит что показывает виджет
☐ У класса есть docstring с описанием роли
☐ Параметры у методов имеют type hints
☐ Метод draw() имеет docstring с параметрами
```

---

## 🧪 Тестирование

```
☐ Виджет отображается на экране
☐ Виджет появляется в правильном месте
☐ Виджет не пересекается с другими виджетами
☐ Текст читаемый (контраст с фоном)
☐ События обрабатываются (если применимо)
☐ Нет ошибок при изменении render_state
☐ Нет ошибок при изменении размера экрана (если критично)
☐ Нет crash'ей при быстрой смене данных
```

---

## 🔌 Интеграция в Renderer

### renderer.py, __init__()

```
☐ Импортирован: from renderer.v3dto.gui_mywidget import MyWidget
☐ Инициализирован: self.my_widget = MyWidget()
   (БЕЗ зависимостей, либо с callback)
☐ Если есть callback, то: self.my_widget = MyWidget(on_change=self._on_change)
```

### renderer.py, _draw_*() методе

```
☐ Вызван draw(): self.my_widget.draw(self.screen, render_state)
   (или без render_state для независимых)
☐ Вызвана в правильном _draw_main/popup/modal методе
☐ Визуально расположен в правильной очерёдности (слои)
```

### renderer.py, _handle_*() методе (если нужны события)

```
☐ if self.my_widget.handle_keydown(event): return True
☐ if self.my_widget.handle_mousebuttondown(event): return True
☐ Проверка в правильном методе (_handle_keyboard_main/etc)
☐ Возвращать True если обработано
```

### renderer.py, callback (если нужен)

```
☐ def _on_parameter_change(self, param_name: str, value: Any) -> None:
☐ Обновить состояние (setattr на SimParams или world)
☐ Логировать изменение (print)
```

---

## 🚫 Проверка "Не Делай"

```
❌ ЗАПРЕЩЕНО:
☐ Нет импорта world, logger, debugger, simparams
☐ Нет обращения к self.world, self.logger, self.debugger
☐ Нет модификации состояния (только вызов callback'а)
☐ Нет зависимостей в __init__() (кроме callback'а)
☐ Нет магических чисел (всё как constants)
☐ Нет вызовов pygame.display.flip() внутри draw()
☐ Нет .blit() прямо на screen (только на свой surface)
☐ Нет импорта других виджетов
☐ Нет создания синглтонов (world(), logger())
☐ Нет создания новых окон pygame
```

---

## 🎨 Стиль Кода

```
☐ Использованы constants для всех "магических чисел"
☐ Методы named с underscore (_draw_border, _handle_input)
☐ Переменные в snake_case
☐ Классы в CamelCase
☐ Docstring'и для всех public методов
☐ Type hints везде где возможно
☐ Максимум 100 символов в строке (рекомендовано)
☐ Пустые строки для разделения секций
```

---

## 📊 Размеры и Позиции

```
☐ Позиция (WIDGET_X, WIDGET_Y) не пересекается с другими
☐ Размер (WIDTH, HEIGHT) разумный
☐ Все координаты внутри экрана (1250x600)
☐ Хорошие отступы от краёв и других виджетов (минимум 5px)
☐ PADDING, BORDER_WIDTH и другие размеры как constants
```

### Текущий макет:
```
Viewport: (5, 5) размер 1240x500
VariablesPanel: (275, 35) размер 700x420
SelectedCreaturePanel: (35, 150) размер 250x300
SelectedCreatureHistory: (4, 505) размер 1243x65

Свободное место для новых виджетов:
- (290, 150)-(1240, 420) - вверху правой половины
- (35, 450)-(275, 575) - слева внизу (осторожно с overlap'ом)
```

---

## 🎯 Перед Финальным Коммитом

```
☐ Работает в основном состоянии (main)
☐ Не ломает существующие виджеты
☐ Нет debug принтов в коде (print, console.log)
☐ Нет временных переменных (temp, xxx, test)
☐ Код отформатирован (правильные отступы, 4 пробела)
☐ Все импорты используются (нет неиспользованных импортов)
☐ Можно запустить приложение: python nnevol.py
☐ Виджет отображается корректно
☐ Нет ошибок в консоли
```

---

## 📋 Git Commit

```
☐ Добавлены все файлы (git add renderer/v3dto/gui_mywidget.py)
☐ Commit message описывает что добавлено
☐ Пример: "Add new MyWidget for displaying creature info"
☐ Если документация изменилась, обновлены и она
```

---

## 🧠 Самопроверка Архитектуры

**Ответь на эти вопросы ДА:**

```
☐ Мой виджет получает данные только из RenderStateDTO?
☐ Мой виджет не импортирует singletons (world, logger, debugger)?
☐ Мой виджет не создаёт побочные эффекты (не модифицирует состояние)?
☐ Мой __init__() не принимает зависимости (кроме callback)?
☐ Все параметры определены как class constants?
☐ Мой draw() использует собственный surface перед blit?
☐ Обработка событий корректно возвращает bool?
☐ Мой виджет полностью изолирован от других виджетов?
☐ Я могу тестировать мой виджет отдельно (мокируя только DTO)?
☐ Новый виджет следует тому же паттерну что существующие?
```

Если все ДА - готово к коммиту! ✅

---

## 📚 Связанная Документация

- **V3DTO_WIDGET_DEVELOPMENT_MANUAL.md** — Полный manual (если что забыл)
- **V3DTO_WIDGET_QUICK_REFERENCE.md** — Краткая шпаргалка
- **V3DTO_WIDGET_CODE_EXAMPLES.md** — Примеры кода
- **V3DTO_WIDGET_ARCHITECTURE_FAQ.md** — Ответы на вопросы

---

## 🎉 Готово!

Если заполнил все ☐ — твой виджет готов к использованию и соответствует архитектуре v3dto!

**Версия:** 1.0  
**Дата:** 2026-01-18  
**Для архитектуры:** v3dto (DTO-based isolation)
